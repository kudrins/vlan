network:
    version: 2
    ethernets:
      ens160:
        dhcp4: no
        dhcp6: no
        addresses:
          - {{ ip }}/24

      ens192:
        dhcp4: no

    vlans:
      ens192.{{ vlan_id }}:
        id: {{ vlan_id }}
        link: ens192
        addresses: [{{ vlan_ip }}/24]
